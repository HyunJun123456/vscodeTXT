IP
공인과 사설이 존재
공유기가 공인 IP를 가지고 있음
공유기 밑단에 있는 IP가 사설 IP임 (내부 IP)

공인 IP를 통해서 공유해서 사용함
100개의 컴퓨터가 있어도 100개의 IP가 필요는 없음
돌려가면서 사용하면 되니깐

내 IP는 고정되지 못함

* DHCP 할당
남는 IP를 부여해준다
다른 IP로 변경부여
-> 서버 운영 불가능!

* 고정 IP -> 비용이 비싸다

* AWS
고정 IP 부여 받는 것 (무료)
-> 고정 IP를 EC2에 연결 안하면 (유료)
-> 연결을 하면 (무료)

